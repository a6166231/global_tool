

export class Picker {
    private _items: Array<string> = []

    constructor(items: Array<string>) {
        this._items = items

        this._refreshReadyList()
    }

    private _refreshReadyList() {
        let readyList = []
        for (let item of this._items) {

        }
    }
}